# Gemini-CLI Setup Guide & Prompt

이 문서는 `gemini-cli` 또는 다른 AI 에이전트가 이 프로젝트(`lip_simulation`)를 처음 접했을 때, 개발 환경을 올바르게 구성하고 실행하기 위한 가이드이자 프롬프트입니다.

---

## 🤖 Role
당신은 Python 기반의 **Streamlit 웹 애플리케이션 배포 및 환경 설정 전문가**입니다.
이 프로젝트는 "퍼스널 컬러 립 시뮬레이션 튜닝 도구"입니다.

## 🎯 Objective
이 프로젝트를 로컬 환경에서 실행 가능하도록 **가상 환경을 구성**하고 **의존성을 설치**한 뒤, **Streamlit 앱을 실행**하는 것입니다.

## 📋 Context & Project Structure
이 프로젝트는 다음과 같은 구조를 가집니다:
- `app.py`: 메인 Streamlit 애플리케이션 파일입니다.
- `requirements.txt`: 의존성 패키지 목록이 명시되어 있습니다. (호환성을 위해 버전이 고정되어 있음)
- `presets/`: 립 시뮬레이션 프리셋 JSON 파일들이 저장되는 곳입니다.
- `modules/`: 렌더링 및 로직 처리를 위한 Python 모듈들입니다.

## 🛠️ Instruction for Gemini-CLI

다음 단계를 순서대로 수행하여 환경을 설정하십시오.

### Step 1: Python 환경 확인
- Python 3.10 이상 버전이 설치되어 있는지 확인하십시오.
- 권장 버전: **Python 3.13** (현재 개발 환경 기준)

### Step 2: 가상 환경(Virtualenv) 생성 및 활성화
- 프로젝트 루트 디렉토리에서 가상 환경을 생성합니다.
  ```bash
  python3 -m venv venv
  source venv/bin/activate  # macOS/Linux
  # .\venv\Scripts\activate  # Windows
  ```

### Step 3: 의존성 패키지 설치 (Critical)
- **반드시** `requirements.txt`를 사용하여 설치해야 합니다.
- `numpy`, `opencv-python` 등의 버전 문제로 이미지가 보이지 않는 이슈가 리포트된 바 있으므로, 고정된 버전을 준수하는 것이 중요합니다.
  ```bash
  pip install -r requirements.txt
  ```

### Step 4: 애플리케이션 실행
- `streamlit` 명령어를 통해 앱을 실행합니다.
  ```bash
  # 가상 환경이 활성화된 상태에서
  streamlit run app.py
  ```

## ⚠️ Troubleshooting Tips (Known Issues)
1. **Zoomed Comparison 이미지 미표시**:
   - 원인: `numpy` 버전이 2.x로 올라가면서 호환성 문제 발생 가능성.
   - 해결: `requirements.txt`에 명시된 `numpy==2.2.6` 등 고정된 버전을 재설치하십시오.

2. **TypeError: float() argument ... not 'NoneType'**:
   - 원인: 프리셋 JSON 파일에 `null` 값이 포함된 경우.
   - 해결: 최신 `app.py`에는 이에 대한 방어 로직이 적용되어 있으므로 코드를 수정할 필요는 없습니다. 최신 커밋을 pull 받았는지 확인하십시오.

---

**Prompt for execution:**
"이 프로젝트의 `requirements.txt`를 기반으로 가상 환경을 설정하고 `app.py`를 실행해줘. 특히 Numpy 버전 호환성에 주의해서 설치해."
